dtmc

const int MAX_N = 3;

const double p = 0.5;

family {
    n : [1..MAX_N];
}

root feature {
    modules test;
}

const start = 0;
const goal = -1;

module test
    s : [-1..n+1] init start;

    for i in [0..n] {
        [] i<=n & s=i -> p: (s'=i+1) + (1-p): (s'=i-1);
    }
endmodule

